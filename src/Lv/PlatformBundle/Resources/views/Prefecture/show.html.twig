<h1>Prefecture</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Prefecturename</th>
            <td>{{ entity.prefectureName }}</td>
        </tr>
        <tr>
            <th>Areaid</th>
            <td>{{ entity.areaId }}</td>
        </tr>
        <tr>
            <th>Areasort</th>
            <td>{{ entity.areaSort }}</td>
        </tr>
        <tr>
            <th>Updated</th>
            <td>{{ entity.updated|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Created</th>
            <td>{{ entity.created|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Deleted</th>
            <td>{{ entity.deleted|date('Y-m-d H:i:s') }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('lv_prefecture') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('lv_prefecture_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('lv_prefecture_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
