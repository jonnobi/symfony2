<h1>Prefecture creation</h1>

<form action="{{ path('lv_prefecture_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('lv_prefecture') }}">
            Back to the list
        </a>
    </li>
</ul>
