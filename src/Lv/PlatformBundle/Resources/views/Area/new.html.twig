<h1>Area creation</h1>

<form action="{{ path('lv_area_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('lv_area') }}">
            Back to the list
        </a>
    </li>
</ul>
